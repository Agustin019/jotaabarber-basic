//const horasDefinitivasEnUnaSolaLinea = Object.values(horasExtraidas[0]).map(hour => hour.hora)
    //console.log(horasDefinitivasEnUnaSolaLinea)

    //const arrayHoras = Object.values(horasExtraidas[0] ?? {} ).map(hour => hour.hora)
    //const arrayDisponibilidad = Object.values(horasExtraidas[0] ?? {} ).map(hour => hour.disponible)
    
   /* const horasDisponibles = horarios.map(el => {
        if(el.horas.disponible === true){
            console.log('la hora esta disponible')
        }else{
            console.log('No hay nada pa dale segui probando')
        }
    })*/
    //const horasNuevas = horarios[0].horas
    //console.log(horasNuevas)

    // Filtrar por turnos que tengan disponibilidad libre
    //const turnosLibres = horarios.filter(hora => hora.horas == true )



    // TIME PICKER


      // OnChange forma 1   
                    //value={selectedDate}    
                    //onChange={handleDateChange}                    
